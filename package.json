{
  "name": "dev-tools",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "codeloop": "pnpm --filter @devtools/cli exec codeloop"
  },
  "engines": {
    "node": ">=22"
  }
}
